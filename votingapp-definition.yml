apiVersion: v1
kind: Pod
metadata: 
    name: voting-app-pod
    lables:
        name: voting-app-pod
        app: demo-voting-app
spec:
    containers:
        - name: voting-app
          image: dockersample/examplevotingapp_vote
          ports:
          - containerPort: 80 
